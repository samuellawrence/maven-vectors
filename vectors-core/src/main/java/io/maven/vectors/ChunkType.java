package io.maven.vectors;

/**
 * Types of code chunks that can be indexed.
 */
public enum ChunkType {
    CLASS,
    INTERFACE,
    ENUM,
    RECORD,
    METHOD,
    CONSTRUCTOR,
    FIELD,
    ANNOTATION
}
